# Murano Product Spec File - Smart Lighbulb Example Application

protocol:
  name: onep
provisioning:
  auth_type: token

resources:
  temperature:
    format: float
  humidity:
    format: float
  state:
    format: integer
    initial: 0
  uptime:
    format: integer
